import COLOR from '../../randomColor.js'
const React = require('react')

class Title extends React.Component {
    render() {
      return <h1 style={{color: COLOR}}>JaydenDev</h1>;
    }
  }

export default Title;